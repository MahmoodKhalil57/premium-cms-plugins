# PremiumCMS Site Kit

Business profile → schema.org JSON-LD, analytics (GA4 / GTM / Cloudflare Web Analytics) behind a cookie-consent banner, Search Console verification and a live Google reviews feed. Configure it under Plugins → Site Kit → Settings; the theme renders everything.
