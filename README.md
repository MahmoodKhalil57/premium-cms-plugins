# PremiumCMS plugins

Marketplace plugins for PremiumCMS. Empty — starting fresh.

```
plugins/<id>/    one plugin: manifest.json, src/, optional frontend/
shared/          code shared between plugins
```

Plugin history before the reset is in this repo's git log.
